# homebrew-polyphony

Homebrew tap for `polyphony`.

Install:

```bash
brew install penso/polyphony/polyphony
```

The formula is updated automatically by Polyphony's release workflow after tagged releases in the
main repository.
